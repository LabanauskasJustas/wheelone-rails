<%= render 'account/shared/page' do |page| %>
  <% page.title t('.section') %>
  <% page.body do %>
    <%= render "filters", q: @q %>
    <turbo-frame id="cars-results">
      <%= render 'index', cars: @cars, pagy: @pagy %>
    </turbo-frame>
  <% end %>
<% end %>
