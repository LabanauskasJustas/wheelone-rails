<% if can? :read, Visualization.new(team: current_team) %>
  <%= render 'account/shared/menu/item', {
    url: main_app.polymorphic_path([:account, current_team, :visualizations]),
    label: t('visualizations.navigation.label'),
  } do |p| %>
    <% p.icon do %>
      <i class="<%= t('visualizations.navigation.icon') %>"></i>
    <% end %>
  <% end %>
<% end %>
